# ai-phishing-detector
Layered phishing detector (TF-IDF baseline + behavioral regex + sender history) built to demonstrate — and close — the gap where content-only classifiers miss well-written AI-generated phishing.
